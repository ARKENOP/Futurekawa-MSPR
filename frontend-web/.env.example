# Base URL de l'API du backend central
VITE_API_BASE_URL=/api/v1

# true = MSW intercepte les appels et renvoie des fixtures (dev hors-ligne)
# false = appels réels vers VITE_API_BASE_URL
VITE_USE_MOCKS=true
